# Tic-Tac-Toe (FutureLearn Exercise)

A simple command line version of tic tac toe programmed in C++

Simply run `sh build.sh` in the `src` folder and the compiled program will be generated in the `build` folder.